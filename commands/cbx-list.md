---
description: List recent cbx jobs in this workspace.
---

List all cbx jobs in the current workspace.

Call `mcp__cbx__cbx_list`. Present a compact table: `jobId`, `status`, `phase`, `attempt`, `updatedAt`. Sort is already newest-first.

For any job the user wants details on, follow up with `/cbx-status <job_id>`.
